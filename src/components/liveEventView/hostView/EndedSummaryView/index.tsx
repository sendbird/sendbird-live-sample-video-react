import { LiveEvent } from "@sendbird/live";

interface EndedSummaryViewProps {
  liveEvent: LiveEvent;
}

export default function EndedSummaryView(props: EndedSummaryViewProps) {
  const {
    liveEvent,
  } = props;


}